{% extends 'base.html.twig' %}

{% block title %}FoodCount{% endblock %}

{% block body %}
    <h1>FoodCount</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ food_count.id }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ food_count.amount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_food_count_index') }}">back to list</a>

    <a href="{{ path('app_food_count_edit', {'id': food_count.id}) }}">edit</a>

    {{ include('food_count/_delete_form.html.twig') }}
{% endblock %}
