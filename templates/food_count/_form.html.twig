{{ form_start(form) }}
    <div class="form-group">
        {{ form_row(form.product, {'attr': {'class': 'form-control'}}) }}
    </div>
    <div class="form-group">
        {{ form_row(form.amount, {'attr': {'class': 'form-control'}}) }}
    </div>
    {# <div class="form-group">
        {{ form_row(form.entry, {'attr': {'class': 'form-control'}}) }}
    </div>     #}
    <button class="btn {{ button_color|default('btn-outline-primary') }}  mt-3">{{ button_label|default('Dodaj') }}</button>
{{ form_end(form) }}




{# {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }} #}
