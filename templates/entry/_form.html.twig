{{ form_start(form) }}
    <div class="form-group">
        {{ form_row(form.name, {'attr': {'class': 'form-control'}}) }}
    </div> 
    <button class="btn {{ button_color|default('btn-primary') }}  mt-3">{{ button_label|default('Dodaj wpis') }}</button>
{{ form_end(form) }}